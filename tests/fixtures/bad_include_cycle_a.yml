betl: 1
name: cycle-a

# Cycle: a includes b which includes a.
include:
  - ./bad_include_cycle_b.yml

pipeline:
  - id: stage_one
    type: dataflow
    steps:
      - id: source
        type: betl.gen_int64
        row_count: 1
