betl: 1
name: param-without-type

parameters:
  load_date:
    # type intentionally missing — must be a parse error
    required: true

pipeline:
  - id: stage_one
    type: dataflow
    steps:
      - id: source
        type: betl.gen_int64
        row_count: 1
