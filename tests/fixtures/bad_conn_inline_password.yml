betl: 1
name: connection-with-inline-password

connections:
  warehouse:
    type: postgres
    # Literal user:password@host with no ${env.X} placeholder anywhere —
    # the parser must reject this per SPEC §5.3 (no inline credentials).
    dsn: postgresql://alice:hunter2@db.example.com:5432/whdb

pipeline:
  - id: stage_one
    type: dataflow
    steps:
      - id: source
        type: betl.gen_int64
        row_count: 1
