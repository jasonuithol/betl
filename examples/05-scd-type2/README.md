# Example 05 — SCD type-2 dimension load

Slowly Changing Dimension type 2 — track row history with
`valid_from` / `valid_to` / `is_current` columns. When a tracked
attribute changes, the old dim row is closed and a new version is
inserted with a fresh surrogate key.

This is the canonical SSIS data-warehouse pattern. betl ships no
dedicated `scd_type2` component because the operation decomposes
cleanly into existing transforms — and keeping the moving parts
visible in the YAML helps when something diverges from expectations.

## The pattern

```
   stg.customer            dim.customer_current  (view: WHERE is_current)
        │                           │
        └──────── join (left) ──────┘
                       │
                       ▼
                   classify   ─→  scd_status: NEW | CHANGED | UNCHANGED
                       │
                       ▼
              conditional_split
                  ╱        ╲
               new          changed
                │              │
                └─── union ────┤
                       │       │
                       │       └─→ close_old_version
                       │              (UPDATE dim SET is_current=false,
                       │               valid_to = batch_ts WHERE customer_sk=$1)
                       │
                       ▼
                 shape_new       (project to dim columns)
                       │
                       ▼
                insert_new_version
                  (INSERT INTO dim.customer (..., valid_from = batch_ts,
                   is_current = TRUE))
```

## How it works

1. **Read staging + current dim image.** `postgres.read` pulls the
   new image of the source-of-truth list (the "batch" you want to
   reflect in the dim). A second `postgres.read` against the
   `dim.customer_current` view loads the in-flight dim rows
   (renaming columns so the join doesn't collide).

2. **Left join** staging × current-dim on the business key. Every
   staging row appears once; the `dim_*` columns are NULL for
   business keys not yet in the dim.

3. **Classify** with a `map` step using a small Lua expression:
    - `dim_sk` is NULL → `NEW`
    - any tracked column differs → `CHANGED`
    - else → `UNCHANGED`

4. **Split** into `new` and `changed` streams via
   `conditional_split`. UNCHANGED rows fall into the default bucket
   with no consumer — they're silently dropped.

5. **Union** `new` + `changed` and feed it into the insert: both
   contribute exactly one new dim row.

6. **INSERT** the new row via `postgres.exec`. The surrogate key is
   auto-generated by the BIGSERIAL column; `valid_from` and
   `is_current` come from the batch timestamp and a constant.

7. **For CHANGED rows only**, run a second `postgres.exec` to UPDATE
   the *old* `dim_sk` — setting `is_current = false` and
   `valid_to = batch_ts`. This step takes its input from
   `route:changed` directly, so it never sees NEW or UNCHANGED rows.

## Setup

```sh
export WAREHOUSE_DSN=postgresql://localhost:5432/warehouse
psql "$WAREHOUSE_DSN" -f sql/dim_setup.sql
```

The setup script creates `dim.customer` (with the surrogate-key
primary key + tracked columns + history columns), the
`dim.customer_current` view, and a `stg.customer` staging table.

## Running a batch

Seed staging with the desired snapshot (any process can do this —
file landing, CDC export, `INSERT INTO stg.customer ...`). Then:

```sh
betl run pipeline.betl.yml --param batch_ts=2026-05-15T00:00:00Z
```

Re-running with the same staging is a no-op (every row classifies
as UNCHANGED). Re-running after a tracked column in staging changes
produces a new dim row at `batch_ts` and closes the old one.

## Notes & limitations

- **One `batch_ts` per run.** Both branches use the same timestamp so
  history stays consistent. Pass the same value to every retry of a
  failed batch.
- **Type-1 ("changing") and "fixed" attributes** aren't covered. Real
  SSIS SCD transforms can mix the two kinds per column. To extend:
  - For type-1, add a `route:type1_only_changed` stream that emits an
    UPDATE-in-place against the current dim_sk.
  - For "fixed", validate in the classify step and route mismatches
    to an error sink.
- **Inferred-member handling** (load a placeholder dim row when the
  fact lands before the dim) — not modelled. The fact-side pipeline
  can use a `*.lookup` with `on_miss: error` and a separate
  late-arriving-dim job.
- **`dtsx2yaml`** translates SSIS Slowly Changing Dimension
  transforms into this exact recipe shape (one-to-many lowering), so
  packages converted from .dtsx land here automatically.
